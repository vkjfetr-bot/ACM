--- a/acm_core_local.py
+++ b/acm_core_local.py
@@
-# Changelog
-# 2025-10-07: Local-files core: contextual baselines, regimes, IF ensemble, drift, spans; CSV "table" helpers (no SQL).
+# Changelog
+# 2025-10-07: Added auto tag detection helper (detect_tags) for CSV-only flows; no CLI tags required.
+# 2025-10-07: Local-files core: contextual baselines, regimes, IF ensemble, drift, spans; CSV "table" helpers (no SQL).
@@
 from typing import List, Dict, Tuple, Optional
 from sklearn.cluster import KMeans
 from sklearn.ensemble import IsolationForest
@@
 def ensure_time_index(df: pd.DataFrame) -> pd.DataFrame:
@@
     raise ValueError("Data must have 'Ts' column or a DatetimeIndex.")
 
 def slice_time(df: pd.DataFrame, start: Optional[str], end: Optional[str]) -> pd.DataFrame:
@@
     return df
+
+def detect_tags(df: pd.DataFrame) -> List[str]:
+    """
+    Auto-detect numeric sensor tags in a CSV.
+    - Excludes the timestamp column 'Ts'
+    - Keeps only columns that are numeric dtype (or coercible by pandas on read)
+    """
+    cols = [c for c in df.columns if c != "Ts"]
+    # If the CSV loader typed numerics as objects, users should re-export; here we trust pandas dtypes.
+    num_cols = [c for c in cols if pd.api.types.is_numeric_dtype(df[c])]
+    if not num_cols:
+        raise ValueError("No numeric sensor columns detected (besides 'Ts').")
+    return num_cols
@@
 def enforce_tags(df: pd.DataFrame, tags: List[str]) -> List[str]:
     missing = [t for t in tags if t not in df.columns]
     if missing: raise ValueError(f"Missing tags in data: {missing}")
     return tags
